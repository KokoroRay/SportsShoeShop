{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Edge",
            "port": 9222,
            "request": "attach",
            "type": "msedge",
            "webRoot": "${workspaceFolder}"
        },
        {
            "type": "pwa-msedge",
            "request": "launch",
            "name": "Launch Microsoft Edge",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}",
            "trace": true
        }
    ]
}
